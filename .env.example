# Optional “cheat sheet” at repo root — real files live per package:
#   backend/.env        (from backend/.env.example)
#   frontend/.env.local (from frontend/.env.example)
#
# Full guide: docs/env-configuration.md

# NEXT_PUBLIC_API_URL=http://localhost:8010
# DATABASE_URL=postgresql+psycopg://USER:PASSWORD@localhost:5432/keyboard_recommender
