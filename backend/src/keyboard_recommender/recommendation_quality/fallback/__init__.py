"""
Fallback recovery (incremental compatibility relaxation for *selection* scoring).

Import concrete symbols from submodules, e.g. `fallback.recovery`, to avoid import cycles
via this package's `__init__.py`.
"""
