services:
  postgres:
    image: postgres:16-alpine
    environment:
      POSTGRES_USER: keyboard
      POSTGRES_PASSWORD: keyboard
      POSTGRES_DB: keyboard_recommender
    ports:
      - "5432:5432"
    volumes:
      - postgres_data:/var/lib/postgresql/data

volumes:
  postgres_data:
